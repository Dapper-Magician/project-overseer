# Open Questions

This file tracks unresolved questions for Project Overseer.

## Architecture

- When should the project add a backend?
- Should the first backend be Python FastAPI, Node, or another option?
- When should local persistence be added?
- Should the first persistence layer be file-backed, SQLite, or browser storage?

## UI / Design

- What exact palette should Project Overseer use?
- What fonts should be used?
- What should the Command Bridge wireframe look like?
- How distinct should each Director room appear?
- Should room transitions be animated in the first polished prototype?

## Director Model

- How much of the original AI Persona Prompt should be extracted into each Director runtime profile?
- Should Director files remain compact or become layered?
- How should Director disagreement be represented in the UI?

## ARES

- When should command syntax be introduced?
- How should ARES protocol cards appear visually?
- How should future connectors map to ARES protocols?
- When should tool inventories be added?

## Memory

- What should the first session summary format be?
- Should estimate calibration be manually updated at first?
- When should artifact indexing become automated?

## Codex Workflow

- How much context should be provided to Codex for each task?
- How should prompts be logged and evaluated?
- What is the best first task after the canon is committed?

## Source Canon

- How should the original Project Overseer AI Persona Prompt be trimmed, indexed, or summarized for runtime use?
- Should the full source prompt remain in `docs/source/` only?
- Should distilled Director files become the default runtime layer?
